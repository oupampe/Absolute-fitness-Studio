# ─────────────────────────────────────────────────────────────
# Absolute Fitness Studio — environment variables
# Copy this file to ".env" and fill in real values.
# The app runs locally with ONLY the two DB URLs + AUTH_SECRET set.
# ─────────────────────────────────────────────────────────────

# Database — Supabase (Connect → ORM tab → Prisma gives you both strings)
# RUNTIME: transaction pooler, port 6543, serverless-safe
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
# MIGRATIONS: direct connection, port 5432, used by Prisma CLI only
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# Auth.js — generate with:  npx auth secret
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"   # set to your live Hostinger domain in production

# Google OAuth (add later — leave blank to disable the Google button)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Facebook OAuth (add later — leave blank to disable the Facebook button)
FACEBOOK_CLIENT_ID=""
FACEBOOK_CLIENT_SECRET=""

# Email for manual payment requests (add later — logs to console if blank)
RESEND_API_KEY=""
PAYMENT_REQUEST_TO_EMAIL="absolutefitnessstudio8@gmail.com"
